/*import 'package:flutter/material.dart';

class FloatingButtonVisibility with ChangeNotifier {
  bool _isVisible = false;
  bool _isClicked = false;

  bool get isVisible => _isVisible;
  bool get isClicked => _isClicked;

  void show() {
    _isVisible = true;
    notifyListeners();
  }

  void hide() {
    _isVisible = false;
    _isClicked = false; // Reseta o clique ao esconder o botão
    notifyListeners();
  }

  void click() {
    _isClicked = true;
    notifyListeners();
  }

  void resetClick() {
    _isClicked = false;
    notifyListeners();
  }
}*/
